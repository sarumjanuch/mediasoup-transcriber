Html Webpage for the mediasoup transcriber server
---

To make the bundled-index.js, run (install browserify or any other bundling tool if necessary):

    browserify index.js -o ../src/bundled-index.js

